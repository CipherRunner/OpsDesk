spring:
  application:
    name: opsdesk

  datasource:
    url: jdbc:postgresql://localhost:5432/opsdesk
    username: opsdesk
    password: opsdesk
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
